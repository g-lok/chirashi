name: Release

on:
  push:
    tags: ["v*"]

env:
  VERSION: ${{ github.ref_name }}

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
            ext: ""
          - goos: linux
            goarch: arm64
            ext: ""
          - goos: darwin
            goarch: amd64
            ext: ""
          - goos: darwin
            goarch: arm64
            ext: ""
          - goos: windows
            goarch: amd64
            ext: .exe

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Build
        env:
          LDFLAGS: "-s -w -X github.com/g-lok/chirashi/cmd.version=${{ env.VERSION }}"
        run: |
          mkdir -p build/releases
          CGO_ENABLED=0 GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} \
            go build -ldflags="$LDFLAGS" \
            -o "build/releases/chirashi-${{ env.VERSION }}-${{ matrix.goos }}-${{ matrix.goarch }}/chirashi${{ matrix.ext }}" .

      - name: Package
        run: |
          cd build/releases
          ARCHIVE="chirashi-${{ env.VERSION }}-${{ matrix.goos }}-${{ matrix.goarch }}"
          if [ "${{ matrix.goos }}" = windows ]; then
            zip -r "$ARCHIVE.zip" "$ARCHIVE/"
          else
            tar czf "$ARCHIVE.tar.gz" "$ARCHIVE/"
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: archive-${{ matrix.goos }}-${{ matrix.goarch }}
          path: |
            build/releases/*.tar.gz
            build/releases/*.zip

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist/
          merge-multiple: true

      - name: Generate unified CHECKSUMS.txt
        run: |
          cd dist
          sha256sum * > CHECKSUMS.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          fail_on_unmatched_files: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
